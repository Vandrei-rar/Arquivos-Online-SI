<table id="datatablesSimple" class="table table-striped">
    <thead>
        <tr>
            <th>Titulo</th>
            <th>Matéria</th>
        </tr>
    </thead>
    <tbody>
        {{#if findResult}}
            {{#each findResult}}
            <form>
                <tr>
                    <td>{{'titulo'}}</td>
                    <td>{{'fk_materia'}}</td>
                    <td class="del-btn">
                        <button type="submit" formmethod="post" formaction="/download/{{titulo}}">AAAAAAAA<i class="fas fa-cloud-download-alt"></i></button>
                    </td>
                </tr>
            </form>
            {{/each}}
        {{/if}}
    </tbody>
</table>